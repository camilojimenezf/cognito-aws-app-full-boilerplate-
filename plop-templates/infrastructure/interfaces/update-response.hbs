/**
 * Response from the provider when updating a {{pascalCase name}}.
 */
export interface {{pascalCase name}}UpdateResponse {
  id: string
  // TODO: add here the fields that your backend returns
}
