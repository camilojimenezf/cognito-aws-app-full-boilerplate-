import { ApiClientFactory } from '@/api/api.client'
import type { List{{pascalCase name}}s } from '@{{name}}/domain/actions/list-{{name}}s.action'
import { type {{pascalCase name}}GetResponse } from '@{{name}}/infrastructure/interfaces/get-response'
import { map{{pascalCase name}}GetResponse } from '@{{name}}/infrastructure/mappers/{{name}}.mapper'

const apiClient = ApiClientFactory.createAuthClient()

export const list{{pascalCase name}}s: List{{pascalCase name}}s = async () => {
  const response = await apiClient.get<{{pascalCase name}}GetResponse[]>(
    '/{{name}}s',
  )
  return response.data.map(map{{pascalCase name}}GetResponse)
}
