import type { {{pascalCase resource}}CreateApiResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/create-response'
import type { {{pascalCase resource}}GetApiResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/get-response'
import type { {{pascalCase resource}}UpdateApiResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/update-response'
import type { I{{pascalCase resource}} } from '@{{feature}}/domain/entities/{{resource}}.entity'

/**
 * Map the creation response to the domain entity.
 */
export function map{{pascalCase resource}}CreateApiResponseTo{{pascalCase resource}}(
  data: {{pascalCase resource}}CreateApiResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the creation fields
  }
}

/**
 * Map the individual read response to the domain entity.
 */
export function map{{pascalCase resource}}GetApiResponseTo{{pascalCase resource}}(
  data: {{pascalCase resource}}GetApiResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the read fields
  }
}

/**
 * Map the update response to the domain entity.
 */
export function map{{pascalCase resource}}UpdateApiResponseTo{{pascalCase resource}}(
  data: {{pascalCase resource}}UpdateApiResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the update fields
  }
}
