import type { {{pascalCase resource}}CreateResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/create-response'
import type { {{pascalCase resource}}GetResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/get-response'
import type { {{pascalCase resource}}UpdateResponse } from '@{{feature}}/infrastructure/interfaces/{{resource}}/update-response'
import type { I{{pascalCase resource}} } from '@{{feature}}/domain/entities/{{resource}}.entity'

/**
 * Map the creation response to the domain entity.
 */
export function map{{pascalCase resource}}CreateResponse(
  data: {{pascalCase resource}}CreateResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the creation fields
  }
}

/**
 * Map the individual read response to the domain entity.
 */
export function map{{pascalCase resource}}GetResponse(
  data: {{pascalCase resource}}GetResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the read fields
  }
}

/**
 * Map the update response to the domain entity.
 */
export function map{{pascalCase resource}}UpdateResponse(
  data: {{pascalCase resource}}UpdateResponse
): I{{pascalCase resource}} {
  return {
    id: data.id,
    // TODO: map here the update fields
  }
}
