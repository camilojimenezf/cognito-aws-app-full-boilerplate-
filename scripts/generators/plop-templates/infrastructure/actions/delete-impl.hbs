import { ApiClientFactory } from '@/api/api.client'
import type { Delete{{pascalCase resource}} } from '@{{feature}}/domain/actions/{{resource}}/delete-{{resource}}.action'

const apiClient = ApiClientFactory.createAuthClient()

export const delete{{pascalCase resource}}: Delete{{pascalCase resource}} = async (
  id: string,
) => {
  await apiClient.delete<void>(`/{{resource}}s/${id}`)
}
