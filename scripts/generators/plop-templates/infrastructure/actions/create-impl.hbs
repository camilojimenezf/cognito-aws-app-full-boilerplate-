import { ApiClientFactory } from '@/api/api.client'
import type { Create{{pascalCase name}} } from '@{{name}}/domain/actions/{{name}}/create-{{name}}.action'
import type { Create{{pascalCase name}}Dto } from '@{{name}}/domain/dtos/{{name}}/create-{{name}}.dto'
import { type {{pascalCase name}}CreateResponse } from '@{{name}}/infrastructure/interfaces/{{name}}/create-response'
import { map{{pascalCase name}}CreateResponse } from '@{{name}}/infrastructure/mappers/{{name}}.mapper'

const apiClient = ApiClientFactory.createAuthClient()

export const create{{pascalCase name}}: Create{{pascalCase name}} = async (
  input: Create{{pascalCase name}}Dto
) => {
  const response = await apiClient.post<{{pascalCase name}}CreateResponse>(
    '/{{name}}s',
    input,
  )
  return map{{pascalCase name}}CreateResponse(response.data)
}
