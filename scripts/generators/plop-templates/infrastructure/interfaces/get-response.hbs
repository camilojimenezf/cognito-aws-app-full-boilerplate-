/**
 * Response from the provider when reading {{pascalCase resource}}s.
 */
export interface {{pascalCase resource}}GetApiResponse {
  id: string
  // TODO: add here the fields that your backend returns
}
