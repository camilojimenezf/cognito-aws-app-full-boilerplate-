/**
 * Response from the provider when updating a {{pascalCase resource}}.
 */
export interface {{pascalCase resource}}UpdateApiResponse {
  id: string
  // TODO: add here the fields that your backend returns
}
