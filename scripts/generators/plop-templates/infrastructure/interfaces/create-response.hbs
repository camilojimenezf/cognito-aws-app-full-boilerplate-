/**
 * Response from the provider when creating a {{pascalCase name}}.
 * Adjust the fields according to your API.
 */
export interface {{pascalCase name}}CreateResponse {
  id: string
  // TODO: add here the fields that your backend returns
}
