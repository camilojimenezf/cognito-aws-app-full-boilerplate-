/**
 * Response from the provider when creating a {{pascalCase resource}}.
 * Adjust the fields according to your API.
 */
export interface {{pascalCase resource}}CreateApiResponse {
  id: string
  // TODO: add here the fields that your backend returns
}
